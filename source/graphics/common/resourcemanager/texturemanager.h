#pragma once
/*
using TextureHandle = uint64_t;
class TextureManager
{
public:
    TextureManager(D3D11Device* device);
    void Shutdown();

    TextureHandle CreateTexture();
    Texture* GetTexture(const TextureHandle handle) const;

private:
    std::unordered_map<TextureHandle, Texture*> m_TextureMap;
    static uint64_t s_TextureNextID;
};*/
